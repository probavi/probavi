{
  "_comment": "Facts about this adapter that its probe response cannot carry: display names, maturity, and the engine versions CI actually restores from. Everything the probe declares (adapter version, engine id, source kinds, pitr) is read from testdata/probe_response.golden instead — never duplicated here. The capabilities generator fails if the source names below do not match the probe exactly, and the integration suite restores from the images listed under verified — the baseline on every run, the rest in the scheduled version matrix and before a release tag — so neither can drift. See docs/capabilities.md and docs/engine-versions.md.",
  "id": "postgres",
  "name": "PostgreSQL",
  "status": "experimental",
  "engine_name": "PostgreSQL",
  "conformance_verified": true,
  "docs": "adapters/postgres/README.md",
  "verified": [
    {
      "engine_version": "14",
      "image": "postgres:14"
    },
    {
      "engine_version": "15",
      "image": "postgres:15"
    },
    {
      "engine_version": "16",
      "image": "postgres:16",
      "baseline": true
    },
    {
      "engine_version": "17",
      "image": "postgres:17"
    },
    {
      "engine_version": "0.8.6-pg17",
      "image": "pgvector/pgvector:0.8.6-pg17"
    }
  ],
  "sources": {
    "pgdump": "One pg_dump file, custom-format (-Fc) or plain SQL (-Fp), stored plain or gzip-compressed",
    "pgdump_dir": "A directory of pg_dump files; the one whose own head records the newest time is restored",
    "pgdump_with_globals": "One pg_dump file plus the cluster globals (roles and grants) loaded before it; either member may be gzip-compressed",
    "pgbackrest": "A pgBackRest filesystem repository (physical restore)"
  }
}
