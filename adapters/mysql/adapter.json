{
  "_comment": "Facts about this adapter that its probe response cannot carry: display names, maturity, and the engine versions CI actually restores from. Everything the probe declares (adapter version, engine id, source kinds, pitr) is read from testdata/probe_response.golden instead — never duplicated here. The capabilities generator fails if the source names below do not match the probe exactly, and the integration suite restores from the images listed under verified — the baseline on every run, the rest in the scheduled version matrix and before a release tag — so neither can drift. See docs/capabilities.md and docs/engine-versions.md.",
  "id": "mysql",
  "name": "MySQL",
  "status": "experimental",
  "since": "0.1.0",
  "engine_name": "MySQL",
  "conformance_verified": true,
  "docs": "adapters/mysql/README.md",
  "versions_checked": "2026-08-27",
  "verified": [
    {
      "engine_version": "8.4",
      "image": "mysql:8.4",
      "supported_until": null,
      "baseline": true
    },
    {
      "engine_version": "9.7",
      "image": "mysql:9.7",
      "supported_until": null
    },
    {
      "engine_version": "8.4.10",
      "image": "percona/percona-server:8.4.10",
      "supported_until": null
    }
  ],
  "sources": {
    "mysqldump": "One mysqldump SQL file, plain or gzip-compressed",
    "mysqldump_dir": "A directory of mysqldump files, plain or gzip-compressed; source.params.select picks which one is restored \u2014 newest by the time each dump records in its own trailer (the default), oldest, or random",
    "mysqldump_with_users": "A directory holding one dump and an accounts-and-grants script, restored together \u2014 the dump named by source.params.dump, or picked by source.params.select; either may be gzip-compressed",
    "xtrabackup": "An unprepared Percona XtraBackup full backup (physical restore)",
    "xtrabackup_with_binlogs": "A directory holding an unprepared XtraBackup full and the binary logs written after it, named by source.params.backup and source.params.binlogs \u2014 the full is restored, then the logs are replayed from the position it ended on, to target.pitr or to the end of the archive"
  }
}
